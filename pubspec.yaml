name: getxify
description: "GetXify: A modern toolkit for building simpler, faster Flutter apps."
version: 5.0.5
homepage: https://github.com/Aniketkhote/getxify
repository: https://github.com/Aniketkhote/getxify
issue_tracker: https://github.com/Aniketkhote/getxify/issues

topics:
  - state-management
  - dependency-injection
  - routing
  - navigation
  - reactive

screenshots:
  - description: "GetXify - A modern toolkit for building simpler, faster Flutter apps."
    path: assets/getxify.png

environment:
  sdk: ^3.12.2
  flutter: ">=3.44.2"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  web: ">=1.1.0 <2.0.0"

  cupertino_ui: ^1.1.1
  material_ui: ^1.4.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
